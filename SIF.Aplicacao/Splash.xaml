<Window x:Class="SIF.Aplicacao.Splash"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:mui="http://firstfloorsoftware.com/ModernUI"
        Background="Transparent" WindowStyle="None" AllowsTransparency="True" SizeToContent="WidthAndHeight" WindowStartupLocation="CenterScreen" ShowInTaskbar="False"
        >

    <Border BorderBrush="{StaticResource Accent}" BorderThickness="2,3,2,3">
        <Grid Background="White" Width="500" Height="170">
            <Grid.RowDefinitions>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>
            <mui:ModernProgressRing IsActive="True" Style="{StaticResource ThreeBounceProgressRingStyle}" Height="100" Width="100"/>
            <TextBlock Grid.Row="1" Text="{Binding Mensagem}" Margin="5"  FontFamily="{StaticResource FixedFontFamily}" FontSize="15" Style="{StaticResource StyleTextBlockTitulo}"
                   HorizontalAlignment="Left"/>
        </Grid>
    </Border>
</Window>
