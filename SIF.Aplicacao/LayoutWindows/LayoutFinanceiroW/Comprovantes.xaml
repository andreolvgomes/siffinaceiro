<mui:ModernWindow x:Class="SIF.Aplicacao.LayoutWindows.LayoutFinanceiroW.Comprovantes"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:mui="http://firstfloorsoftware.com/ModernUI"
        xmlns:commom="clr-namespace:SIF.Commom;assembly=SIF.Commom"
        IsTitleVisible="True" Style="{StaticResource BlankWindow}" Title="Comprovantes" SizeToContent="Height" WindowStartupLocation="CenterOwner"
        >

    <Window.Resources>
        <BooleanToVisibilityConverter x:Key="booleanToVisibilityConverter"/>
    </Window.Resources>

    <Grid Grid.Row="2">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <Grid Grid.Row="1">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>
            <ListBox Grid.Column="0" Width="150" Margin="2">
            </ListBox>
            <Grid Grid.Column="1" Height="500" Margin="2">
                
            </Grid>
        </Grid>
    </Grid>
    </mui:ModernWindow>
