<UserControl x:Class="SIF.Aplicacao.Layouts.LayoutMovimentacoes"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:imageIcon="clr-namespace:SIF.Aplicacao.Layouts"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             >
    <Grid Style="{StaticResource ContentRoot}">
        <ScrollViewer>
            <StackPanel>
                <TextBlock Text="LANÇAMENTOS" Style="{StaticResource TextBlockHeading}" Margin="0,10,0,0"/>
                <Separator/>
                <ListView Style="{StaticResource MyListView}">
                    <imageIcon:ImageIcon Grid.Row="0" Titulo="Movimentação"  IconSource="/Icones/movimentacaofinan.png" MouseLeftButtonDown="ImageIcon_MouseLeftButtonDown_1"/>
                </ListView>
            </StackPanel>
        </ScrollViewer>
    </Grid>
</UserControl>
