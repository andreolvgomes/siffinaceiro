<UserControl x:Class="SIF.Aplicacao.Layouts.ImageIcon"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             >
    <!--<Grid x:Name="grdMain" ToolTip="{Binding ToolTipDescricao}" MaxHeight="120" MaxWidth="120">
        <Grid.RowDefinitions>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>
        <Image Grid.Row="0" Source="{Binding IconSource}"/>
        <TextBlock Grid.Row="1" Text="{Binding Titulo}" HorizontalAlignment="Center" TextWrapping="Wrap"/>
    </Grid>-->
    <Grid x:Name="PART_Grid" Background="Transparent">
        <Grid.RowDefinitions>
            <RowDefinition/>
            <RowDefinition/>
        </Grid.RowDefinitions>
        <Image Source="{Binding IconSource}" Width="76" Height="76"/>
        <TextBlock Grid.Row="1" HorizontalAlignment="Center"
									   TextWrapping="Wrap"
									   TextAlignment="Center"
									   Text="{Binding Titulo}"/>
    </Grid>
</UserControl>
