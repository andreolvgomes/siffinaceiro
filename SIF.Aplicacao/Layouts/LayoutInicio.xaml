<UserControl x:Class="SIF.Aplicacao.Layouts.LayoutInicio"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             >
    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>            
        </Grid.RowDefinitions>
        <TextBlock Grid.Row="0" Text="XXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXX" Style="{StaticResource Heading1}"/>
        <TextBlock Grid.Row="1" Text="{Binding DataHora}" Style="{StaticResource Heading1}" HorizontalAlignment="Right"/>
    </Grid>
</UserControl>
